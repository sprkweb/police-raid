# Credentials for the active NetworkService implementation (see createNetworkService.ts).
# Current transport expects a Metered Realtime publishable key:
#   publish + subscribe + presence + send; channel pattern * or police-raid/*
VITE_METERED_API_KEY=
